---
description: Stop the Conscio Awake daemon for this host's space.
---

Stop the Awake daemon for this host's Conscio space (terminate the
`conscio daemon` process bound to the configured `--storage`). Confirm it
stopped in one line.
